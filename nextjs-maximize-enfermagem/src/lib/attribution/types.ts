/**
 * Re-export do shape compartilhado em domain/. Coloca o tipo perto do
 * código de capture/storage pra leitor não precisar pular pra
 * lib/diagnostico só pra encontrar a interface.
 */
export type { AttributionData } from "@/lib/diagnostico/types";
