# URL da API (Nest). Padrão: http://localhost:3000
# A API precisa estar rodando para a página de concursos funcionar.
NEXT_PUBLIC_API_URL=http://localhost:3000

# Revalidação sob demanda. A API chama /api/revalidate quando um concurso é alterado.
# Deve ser o mesmo valor configurado na API (REVALIDATE_SECRET).
REVALIDATE_SECRET=
